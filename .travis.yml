dist: trusty
sudo: false
language: cpp
compiler: gcc

notifications:
  email: false
  
before_script:
  - sudo apt-get install -y libsndfile1-dev libxrandr-dev libudev-dev libopenal-dev build-essential

script:
  - ./console build
